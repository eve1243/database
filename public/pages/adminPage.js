// public/pages/adminPage.js

/**
 * Initialisiert die Logik für die Admin-Sektion.
 * (Aktuell nur ein Platzhalter)
 * @param {HTMLElement} adminSection - Das HTML-Element der Admin-Sektion.
 */
export function initAdminPage(adminSection) {
    console.log("Admin-Seite initialisiert.");
    // Hier könnten später Event-Listener oder Logik für Verwaltungsfunktionen hinzugefügt werden.
    // Beispiel: adminSection.innerHTML = "Inhalt für Admin hier...";
}
